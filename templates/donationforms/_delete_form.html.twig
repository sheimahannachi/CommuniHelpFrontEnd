<form method="post" action="{{ path('app_donationforms_delete', {'id': donationform.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ donationform.id) }}">
    <button class="btn">Delete</button>
</form>
