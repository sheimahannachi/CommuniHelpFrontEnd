<form method="post" action="{{ path('app_livraison_p_delete', {'id': livraison_p.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livraison_p.id) }}">
    <button class="btn">Delete</button>
</form>
